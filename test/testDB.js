var should = require('should');
var DB = require('../models/DB.js').DB;

describe('DB', function(){


/*  before (function (done) {
    DB.destroy(function(err, results){
      done();
    });
  });

  after(function (done) {
    DB.init(function(err, results){
      done();
    });
  });


  describe('init', function(){
    it('should create all tables and database', function(done){
      DB.init(function(err, results){
        should.not.exist(err);
        should.exist(results);
        results.created.should.equal(1);
        done();
      });
    })
  });

  describe('destroy', function(){
    it('should destroy all tables and database', function(done){
      DB.destroy(function(err, results){
        should.not.exist(err);
        should.exist(results);
        results.dropped.should.equal(1);
        done();
      });
    })
  });*/
});